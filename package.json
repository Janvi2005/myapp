{
  "name": "ocr-multilingual",
  "version": "1.0.0",
  "description": "OCR software supporting Gujarati, English, and Hindi",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "build": "npm run build --workspace=client && npm run build --workspace=server",
    "start": "npm start --workspace=server",
    "prod": "npm run build && npm run prod --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
